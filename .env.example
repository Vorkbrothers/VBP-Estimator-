# Port the server listens on (Railway sets this automatically)
PORT=3001

# Path for persistent storage (database + uploaded files).
# On Railway: set this to your volume mount path, e.g. /data
# Locally: leave unset (defaults to ./backend/data and ./backend/uploads)
DATA_PATH=/data
